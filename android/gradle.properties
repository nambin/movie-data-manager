# Committed — no secrets in this file. See secrets.properties (gitignored)
# for the GitHub PAT / Gemini key / TMDB key. See android/README.md.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# AGP 8.7.3 builds fine against compileSdk 36 (verified: full build + unit
# tests green) but only warns "not officially tested past 35" every run.
# Silences that warning; revisit once a newer AGP officially supports 36.
android.suppressUnsupportedCompileSdk=36
android.defaults.buildfeatures.resvalues=true
android.sdk.defaultTargetSdkToCompileSdkIfUnset=false
android.enableAppCompileTimeRClass=false
android.usesSdkInManifest.disallowed=false
android.uniquePackageNames=false
android.dependency.useConstraints=true
android.r8.strictFullModeForKeepRules=false
android.r8.optimizedResourceShrinking=false
android.builtInKotlin=false
android.newDsl=false
