{
  "name": "movie-data-manager-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static web editor for the movie collection YAML.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "build": "node scripts/build.mjs",
    "build:dev": "node scripts/build-dev.mjs",
    "watch": "node scripts/build.mjs --watch",
    "capture:fixtures": "node scripts/capture-fixtures.mjs",
    "curate:awards": "node cli/curate-awards.mjs",
    "reconcile:awards": "node cli/reconcile-awards.mjs"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
